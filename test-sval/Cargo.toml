[package]
name = "test-sval"
publish = false
edition = "2024"

[dependencies]

[dev-dependencies]
fnv = "1.0"
ringmap = { path = "..", features = ["sval"] }
sval = { version = "2", features = ["derive"] }
sval_test = "2"
